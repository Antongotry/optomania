{% if products %}
	<div class="row mt-3 mt-md-0 p-3 p-md-0 pt-xl-3 g-2 g-md-3 pr-module">
		<div class="pr-module-header fsz-22 dark-text fw-700 text-center text-md-start mt-0 mt-md-2 mb-2">{{ heading_title }}</div>
		
			{% for product in products %}
			<div class="pr-module-item pr-module-col" data-product-id="{{ product.product_id }}">
				<div class="content-block h-100 d-flex flex-column">
					<div class="pr-module-img position-relative">
						<a href="{{ product.href }}">
							<img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-fluid" />
						</a>
						<div class="pr-module-btns d-flex flex-column position-absolute">
							<button type="button" aria-label="Wishlist" class="pr-btn pr-btn-outline pr-btn-outline-secondary pr-btn-with-icon br-4 pr-wishlist-btn" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}');">
								<span class="pr-btn-icon pr-btn-icon-wishlist"></span>
							</button>
							<button type="button" aria-label="Compare" class="pr-btn pr-btn-outline pr-btn-outline-secondary pr-btn-with-icon br-4 pr-compare-btn" title="{{ button_compare }}" onclick="compare.add('{{ product.product_id }}');">
								<span class="pr-btn-icon pr-btn-icon-compare"></span>
							</button>
						</div>
					</div>
					<div class="pr-module-caption d-flex flex-column justify-content-between py-md-2 h-100">
						<div class="pr-module-info">
							<a href="{{ product.href }}" class="pr-module-title dark-text fw-700">{{ product.name }}</a>
							{% if product.rating is not same as(false) %}
								<div class="pr-module-rating d-flex align-items-center mt-2 mb-1 br-4">
									<div class="pr-module-rating-stars d-flex align-items-center">
										{% for i in 1..5 %}
                                            {% if product.rating < i %}
                                                <span class="pr-module-rating-star"></span>
                                            {% else %}
                                                <span class="pr-module-rating-star pr-module-rating-star-is"></span>
                                            {% endif %}
                                        {% endfor %}
									</div>
								</div>
							{% endif %}
						</div>
						{% if product.price %}
						<div class="pr-module-price-box py-2 mt-auto">
							{% if product.tax %}
								<div class="price-tax fw-300 fsz-12 light-text">
									{{ text_tax }} {{ product.tax }}
								</div>
							{% endif %}
							{% if not product.special %}
								<span class="pr-module-price fw-800 dark-text mt-1">{{ product.price }}</span>
							{% else %}
								<span class="pr-module-price-old light-text fsz-14">{{ product.price }}</span>
                                <span class="pr-module-price fw-800 dark-text mt-1">{{ product.special }}</span>
							{% endif %}
						</div>
						{% endif %}
					</div>
				</div>
			</div>
			{% endfor %}
	</div>
  {% endif %}
